export type EventCategory = "Začátek semestru" | "Konec semestru" | "Výuka" | "Registrace předmětů" | "Zápis předmětů" | "Změny zápisu" | "Zveřejnění rozvrhu" | "Zkouškové období" | "Prázdniny" | "Státní závěrečné zkoušky" | "Odevzdání závěrečných prací" | "Imatrikulace" | "Promoce" | "Praxe" | "Fakultní akce" | "Ostatní";
export type StudyYear = 1 | 2 | 3 | 4 | 5 | 6;
export type AcademicEvent = { id: string; externalId?: string; title: string; category: EventCategory; school: string; faculty: string; start: string; end?: string; allDay?: boolean; timezone?: string; academicYear?: string; studyYears?: StudyYear[]; source: string; sourceId?: string; sourceUrl: string; sourceDocumentTitle?: string; sourcePage?: number; sourceUpdatedAt?: string; updatedAt: string; lastVerifiedAt: string; description: string; confidence?: number; status?: "approved" | "pending" | "archived"; changeState?: "unchanged" | "changed" | "cancelled"; scope?: "city" | "university" | "faculty" | "programme" | "national" | "brno"; cityId?: string; universityId?: string; facultyId?: string; programmeId?: string };
export type VerificationStatus = "verified" | "needs_review" | "unavailable";
export type Place = { id: string; cityId?: string; name: string; category: "Studovna" | "Knihovna" | "Menza" | "Tisk" | "Kavárna" | "Sport" | "Servis" | "Ostatní"; address: string; hours?: string; website: string; sourceUrl: string; lastVerifiedAt: string; openingHoursVerifiedAt?: string; verificationStatus: VerificationStatus; osmId?: string; lat: number; lng: number; note: string; whyVisit?: string; priceLevel?: string; studentDiscount?: string; universityIds?: string[]; facultyIds?: string[] };
export type Offer = { id: string; cityIds?: string[]; title: string; category: "Jídlo" | "Software" | "Doprava" | "Kultura" | "Sport"; partner: string; discount: string; validTo: string; conditions: string; url: string; sourceUrl: string; lastVerifiedAt: string; requiresIsic: boolean; sponsored: boolean; affiliate: boolean; featured: boolean; universityIds?: string[]; facultyIds?: string[]; lat?: number; lng?: number };
export type JobRewardUnit = "hour" | "day" | "shift" | "month" | "agreement" | "fixed" | "volunteer";
export type Job = { id: string; externalId?: string; providerKey?: string; cityId?: string; locationMode?: "onsite" | "hybrid" | "remote"; title: string; company?: string; field: "IT" | "Elektro" | "Administrativa" | "Gastro" | "Ostatní"; type: "DPP" | "DPČ" | "DPP / DPČ" | "Stáž" | "Jednorázově" | "Pracovní poměr"; location: string; reward?: number; rewardMin?: number; rewardMax?: number; rewardCurrency?: "CZK" | "EUR" | "USD" | "GBP"; rewardUnit?: JobRewardUnit; workload: string; contact?: string; applyUrl?: string; sourceUrl?: string; sourceLabel?: string; lastVerifiedAt: string; validTo?: string; description: string; featured: boolean; status: "approved" | "pending" | "rejected"; universityIds?: string[]; facultyIds?: string[] };
export type University = { id: string; slug: string; name: string; shortName: string; color: string; officialUrl: string; active: boolean; lastVerifiedAt: string };
export type Faculty = { id: string; slug: string; universityId: string; name: string; shortName: string; officialUrl: string; active: boolean; lastVerifiedAt: string };
export type AcademicCatalog = { universities: University[]; faculties: Faculty[] };
export type StudentPreference = { version: 4; cityId: string; universityId: string | null; facultyId: string | null; studyYear: StudyYear | null; studyYearCycleStart: number | null; completed: boolean };
